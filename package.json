{
  "name": "goal-quest-api",
  "version": "1.0.0",
  "description": "An API for our Qoal Quest app- An app to self-learn skills without human assistance",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "cross-env NODE_ENV=test mocha --timeout 120s --exit",
    "test-single": "cross-env NODE_ENV=test mocha --timeout 10000ms --exit",
    "start": "cross-env NODE_ENV=production node server",
    "dev": "cross-env NODE_ENV=development nodemon server",
    "data:import": "node seeder",
    "data:destroy": "node seeder -d"
  },
  "author": "Idrees Awan",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "colors": "^1.4.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-async-handler": "^1.2.0",
    "jsonwebtoken": "^8.5.1",
    "mongodb-memory-server": "^8.0.3",
    "mongoose": "^6.0.12",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "mocha": "^9.1.3",
    "nodemon": "^2.0.14",
    "supertest": "^6.1.6"
  }
}
